#!/usr/bin/env sh
set -eu

run_check() {
  if output=$("$@" 2>&1); then
    return 0
  else
    status=$?
    printf '%s\n' "$output" >&2
    return "$status"
  fi
}

run_check bun run format:check
run_check bun run lint
