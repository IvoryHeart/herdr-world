# Specification authoring

The immutable numbered-spec template is retired. Use
[OpenSpec](../../openspec/config.yaml) and the [knowledge map](../knowledge-map.md).

State the problem, observable behavior, failure scenarios and concrete constraints.
Keep one current spec per capability and one change per coherent outcome.
Update or supersede it in the same PR when direction changes; no numbered extension is required.
