Use world-shape-work to clarify the authorized outcome from the task and current source.
This is a read-only product activation. Return requirements.ready with acceptance as an
array of {id, criterion}; each criterion must be observable and within the owner's request.
Put the user problem, non-goals, assumptions and required OpenSpec updates in summary.
The implementer makes any authorized durable updates. Do not create a second PRD store.
If supervisor context already contains acceptance criteria, return them unchanged.
Use task.blocked only for a material missing owner decision; do not invent user research,
roadmap priorities, permissions or product contracts. Existing authorization persists.
