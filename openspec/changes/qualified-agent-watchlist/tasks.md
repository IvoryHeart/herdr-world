## 1. Define and expose shared watch state

- [x] 1.1 Add a pure service registry keyed by connection ID, generation and terminal-backed World ID, capped at 128 records with bounded labels; verify duplicate Pin, 128th/129th Pin, exact Unpin and agent↔terminal classification cases.
- [x] 1.2 Add `world.watchlist.list/pin/unpin` as bridge-global methods in `server/src/index.ts`. Validate pin against the named ready runtime and exact live pane, reject unknown/stale targets without default-host fallback, and allow exact Unpin of an unavailable record. Verify no Herdr mutation or disk/browser persistence is introduced.
- [x] 1.3 Retire records for a replaced connection generation, broadcast `world_watchlist_changed` with a process-local revision after each real mutation, and return revisioned List data. Verify simultaneous browsers, duplicate mutations and reconnect do not resurrect an older list.
- [x] 1.4 Feed current-generation records from the service registry into `WorldSnapshotService` independently of the eight browser priorities. Match exact terminal IDs in each host's raw `pane.list`; reserve every uniquely matched watched pane and its workspace/tab ancestors ahead of ordinary relevance and group reservations while retaining the 512/2,048/4,096/4,096 record caps and existing raw coverage. Verify all 128 watches fit even with more than 4,096 panes, and malformed or ambiguous ancestry is unresolved rather than guessed.
- [x] 1.5 Return a per-host watch-admission summary with watchlist revision and explicit registered/missing/unresolved/matched/admitted/admission-failed counts. Classify each record exactly once from raw pane candidates: zero is missing, duplicate terminal IDs or invalid ancestry are unresolved, one valid candidate is matched, and a matched record without complete admitted ancestry is admission-failed. Assert `registered = missing + unresolved + matched` and `matched = admitted + admission_failed` on a fresh host; a duplicate-ID fixture with one record must yield registered 1, unresolved 1, missing 0, matched 0 and admitted 0. Invalidate and queue a follow-up snapshot when pins change during an in-flight fetch. Verify older admission revisions never classify a newly pinned pane as missing and stale hosts do not claim current classification counts.

## 2. Consume and present pins

- [x] 2.1 Add a focused browser watchlist store that fetches List on bridge connection and change invalidation, ignores older replies/revisions within a connection, invalidates old requests/revisions on reconnect, and marks cached state unverified while disconnected. Verify a second browser observes Pin/Unpin without manually refreshing and a restarted service's empty revision-zero list replaces old state.
- [x] 2.2 Resolve each record through current `WorldObject` before an action; verify a missing/stale pane is visibly unavailable and equal native IDs on another host or generation never resolve the pin.
- [x] 2.3 Add accessible Pin/Unpin controls to the common selected-entity context of Office, Tree and Graph, with capacity and disconnected feedback. Verify mouse, keyboard, touch and compact operation use the same service mutation.
- [x] 2.4 Add a shared Pinned only toggle alongside common visual search; verify it follows view navigation and selected-host switching, filters only the selected host, and does not change Spaces focus or the underlying all-host observation.
- [x] 2.5 Rank watched Tree/Graph spaces and leaves and Office occupants ahead of unpinned peers within the existing view bounds, retaining host/space and room/desk ancestry. Verify search, disclosure, mixed-state tabs and Graph position pins remain correct. Show raw observed, registered, current matched/admitted, displayed, view-omitted, missing, unresolved and admission-failed counts from the disjoint service categories without treating search exclusions or stale/pending admissions as missing.

## 3. Accept and deliver the workflow

- [x] 3.1 Exercise two browser clients and two ready hosts with colliding native IDs, page reload, service restart, host switch, disconnect, missing pane and generation replacement in focused service/store and mounted-browser checks. Add an over-bound host with more than 4,096 panes, 128 watches including a late low-ranked pane, and more than 16 watched panes in one space; verify service admission and full ancestry, exact view overflow counts, and no watched action rebinding.
- [x] 3.2 Update Features and affected source map for the shared process lifetime, add an Unreleased entry, and distinguish watch pins from workspace and Graph position pins in labels and documentation.
- [x] 3.3 Synchronize the accepted delta into current `world-surfaces`, run focused checks and `bun run check`, inspect final diff/history for generated output and sensitive data, then open a ready PR with exact evidence for independent review.
