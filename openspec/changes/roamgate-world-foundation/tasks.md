## 1. Foundation import

- [x] 1.1 Record Roamgate commit `81c506e6135f5f3b47f7042252ffdac0ec2bf679` in branch ancestry, replace the active application tree with that source while retaining World planning/history, and verify the imported upstream quick suite passes unchanged
- [x] 1.2 Rebrand package, executable, service, environment, data-directory, browser-storage, update and UI identities to Herdr World, and verify identity audits find no executable Roamgate product surface while required attribution remains
- [x] 1.3 Adapt the standalone installer and Herdr plugin to install/start/status/restart/uninstall the World binary, and verify focused installer, service and plugin contract tests

## 2. Managed runtime projection

- [x] 2.1 Preserve and characterize Roamgate local/SSH profile lifecycle, dual-socket forwarding, retry classification and generation isolation under World identities, and verify the focused connection/server suites
- [x] 2.2 Add a bounded aggregate snapshot/status/event interface for every ready connection, and verify tests cover two connections, colliding native IDs, one failed host and retired generations
- [x] 2.3 Add the browser aggregate runtime store with connection-qualified selection and action routing, and verify delayed or stale data cannot cross or replace another connection

## 3. First-class World experience

- [x] 3.1 Port the shared WorldObject projection onto the aggregate Roamgate runtime store, and verify deterministic host-space-agent-or-terminal hierarchy tests for local and SSH profiles
- [x] 3.2 Integrate native Spaces and World routes into one Roamgate-derived application frame without mounting a second app; keep the Spaces workspace navigator, focused tab strip and annotations control common to every view, route navigator and focused-tab selections through the same exact qualified docked Inspector admission independently of Office's presentation preference, replace Graph's duplicate desktop outline and stage toolbar, place the single view selector, bounded selected-host/runtime summary and shared search plus applicable Fit/zoom controls in the inherited top bar without a stacked World navigation or status bar, and verify canonical navigation plus desktop/mobile accessibility tests
- [x] 3.3 Complete the retained Pixel Office migration in tasks 10.1–10.8 and verify the defining scene, layout, operations and responsive behavior over the aggregate store's selected-host projection
- [x] 3.4 Characterize the checkpoint World-to-Spaces Files, Changes and Agent History handoff, and verify an aggregate selection cannot open resources from a different host or checkout
- [x] 3.5 Complete the connected Tree and spatial Graph migrations in tasks 11.1–12.4 and verify both distinct presentations, guarded actions and compact/accessibility equivalents after the Office seam passes

## 4. Distribution and knowledge

- [x] 4.1 Remove the retired Rust bridge, vendored compatibility crate, browser-federation configuration and Capacitor build from active distribution paths, and verify no release or startup command references them
- [x] 4.2 Generate complete dependency/source notices, record Roamgate lineage, and verify notice and upstream-reference checks are byte-clean
- [x] 4.3 Reconcile README, feature, architecture, deployment, development, packaging, release and knowledge-map documentation with the one-service World product and verify every documented command exists
- [x] 4.4 Synchronize the four affected current capability specs with the completed foundation and view-migration behavior, and pass strict OpenSpec validation

## 5. Acceptance and delivery

- [x] 5.1 Run formatting, lint, type checking, unit/integration tests and production builds through the repository's final check command and record exact results
- [x] 5.2 Exercise the compiled World application against a local Herdr plus the deterministic SSH fixture, verifying connection management, aggregate World observation, terminal input, Files, Changes and Agent History
- [x] 5.3 Run responsive mounted-browser acceptance on Office, Tree and Graph at desktop and phone sizes, and publish privacy-safe synthetic captures that demonstrate the delivered compositions and operational contexts
- [x] 5.4 Review the final diff and history for secrets, unrelated generated output and stale product identities; push the scoped foundation implementation and return PR #93 to ready status for independent review

## 6. Review corrections

- [x] 6.1 Enforce automatic request-authority and same-origin admission for privileged browser WebSocket traffic without adding user-managed allow-lists, with rejection coverage
- [x] 6.2 Preserve the selected connection lease through World-to-Spaces handoff and suppress Spaces operational shortcuts while that mounted view is hidden, with browser-level regressions
- [x] 6.3 Make retained aggregate observations stale and non-actionable on browser disconnect or refresh failure, and coalesce invalidations without starving admitted snapshots
- [x] 6.4 Generate platform-independent dependency inventory plus bundled copyright and licence texts, and verify release archives contain them
- [x] 6.5 Inject the reviewed build version into the frontend and document the replacement installer cutover at the foundation checkpoint
- [x] 6.6 Run focused regressions, the complete repository check and packaged-distribution inspection, and publish the foundation checkpoint for review

## 7. Re-review corrections

- [x] 7.1 Extend automatic browser admission across privileged HTTP and WebSocket traffic, add one exact loopback-proxy public origin plus a World-owned auth cookie, and verify unit and production-process coverage
- [x] 7.2 Bind World-to-Spaces focus to its original host without cross-host reconnect retry, close and disable the mounted Spaces command palette while hidden, and verify store and Chromium regressions
- [x] 7.3 Include the deterministic `lru_map` README licence section, replace the old installation shutdown guidance with the actual target-by-target asynchronous plugin procedure, and verify notices and documentation checks
- [x] 7.4 Run focused regressions and the complete repository check, refresh the local test service, respond to every foundation re-review thread, and record the green checkpoint

## 8. Shared visual context, proven in Office

- [x] 8.1 Complete the qualified WorldObject presenter input with bounded admitted task summaries, agent/model and state labels, focus, active/ready-inactive/reconnecting/offline-stale host state and action capabilities; retain selected identity with its observed generation and verify missing metadata is never inferred or rebound across generations
- [x] 8.2 Complete one reusable shell-owned Inspector surface that leaves the Office stage dimensions unchanged, composes compact identity and applicable resources under one lifecycle without a duplicate actionable profile card, and uses read-only activation context only for inactive or stale selections; verify docked and floating presentations admit identity/resources atomically and delayed, rejected or generation-replaced targets cannot expose mismatched content
- [x] 8.3 Reuse the existing Terminal, Files, Changes and Agent History implementations in independently keyed Inspector UI sessions without cloning the application/runtime store or transport; default new terminal-capable sessions to Terminal, retain tab and resource selection per session, synchronize workspace-qualified review drafts with the common annotation panel, remove Open in Spaces, and verify two Inspectors can hold different resources while spaces and non-agent panes retain their applicable tabs
- [x] 8.4 Require a valid restored/default managed profile before showing a visual view, make Office the primary default surface while retaining Spaces as the profile-management and operational workspace, project only the selected host into each visual view and expose Switch now only for a transient outgoing context, and route Office Inspector, terminal and Spaces actions only through the exact selected connection/generation; verify view or entity selection never changes hosts, colliding native identifiers cannot cross hosts, and switching follows the existing teardown/revalidation lifecycle
- [x] 8.5 Add validated Office preference storage and bounded Office error containment, and verify a malformed presenter does not stop Spaces, runtime observation, Inspector state or the current terminal

The former task-summary producer and agent/pane watchlist are explicitly outside this foundation
delivery. Their restoration and expanded live local/SSH acceptance are tracked by GitHub issue #95.

## 9. Shared live Inspector conversations

- [x] 9.1 Replace the terminal-only floating presentation with the reusable Inspector surface and one qualified registry entry per entity; move Office room alignment, long-title, Inspector and observability controls into the common settings menu without a scene toolbar or mobile/Zen strip; add the persisted Office Docked/Floating opening preference with Floating as its fresh default so subsequent Office opens use the chosen docked target or distinct cascaded windows without rearranging existing presentations; verify selecting an existing entity focuses its current presentation, and ensure the same entity never acquires duplicate resource or terminal ownership
- [x] 9.2 Key the bounded registry and every independent tab/resource/geometry state by selected connection, runtime generation and entity identity inside the inherited browser lease; verify up to five floating Inspectors plus one docked Inspector add no connection-independent client, second WebSocket, SSH tunnel, application store, Herdr Web resource implementation or per-host terminal manager
- [x] 9.3 Implement movable/resizable/focusable floating Inspector windows with × and Dock in icons plus Dock out on the docked Inspector; allow a freely moved desktop Inspector to cross the complete application viewport while remaining recoverable, keep compact windows fully contained and place the visual resize affordance directly on the window boundary as a small corner bracket with an accessible drag target; atomically transfer the complete Inspector state, swap an occupied dock without increasing the window count, preserve one qualified terminal attachment and verify close, duplicate and sixth-window behavior
- [x] 9.4 Restore qualified connectors from every visible Inspector to its desk/agent/node anchor and portal-safe focus of the exact Herdr pane; verify terminals refit and accept input after move, resize, dock, undock and browser-focus return, mobile input controls follow the active pane, and mounted-but-hidden Spaces never attaches a competing terminal
- [x] 9.5 Preserve selected-host Inspector conversations through projection refresh and visual navigation, suspend every visual presentation while native Spaces is visible, hand the exact selected terminal between mutually exclusive presenters without recreating its Herdr session, restore retained Inspector state on return, retire every outgoing context before explicit host switching or generation replacement, and verify generation fencing plus a compact layout with one accessible usable Inspector

## 10. Pixel Office migration

- [x] 10.1 Restore the retained Office source baseline from World commit `9c8f650853ad2d598d476dac1eecdeaea16716c6`: pinned PixiJS dependency, character assets and licence, PixelOfficeCanvas, geometry, layout publication, renderer lifecycle/resources, scene signature, selection, semantic targets, observability presentation and focused tests; verify provenance/notices and reject a second renderer implementation
- [x] 10.2 Complete the retained Office projection input over WorldObject while preserving its presenter output contract, normalized bounded desk labels, qualified rooms, tabs/desks, agents, receptions and Agent Bar placement; prioritize the selected operational host within presentation bounds and verify mixed-state shared tabs plus beyond-eight-desk cases seat or stand only bounded working/unknown room-local agents, keep blocked and idle/done destinations, report exact omissions and retain nonduplicated identity and ancestry
- [x] 10.3 Integrate the retained deterministic Office geometry/publication without redesign, preserving content-sized two-to-eight-seat rooms, title/action containment, natural row packing, alignment, logical scrolling and stale-render acknowledgement; verify the retained dense and long-title fixtures
- [x] 10.4 Mount the retained Pixi Office renderer in the Roamgate-derived route without depending on hidden Spaces layout DOM, remove the substitute HTML/CSS scene, and preserve CEO Office, host receptions, boards, Agent Bar, roads, rooms, desks, characters and bounded overflow presentation; compare retained scene signatures and synthetic desktop fixtures with the source baseline
- [x] 10.5 Restore bounded hover task-summary callouts without a duplicate persistent selected-agent badge, bounded completion rendezvous markers/notices with exact selected-host terminal inspection and retained unseen state when the host is inactive or activation fails, 48px semantic targets and compact Agents/Rooms/Desks selection, and verify pointer, keyboard, touch and screen-reader parity
- [x] 10.6 Restore capability-gated room create/rename/close and room-local seat creation only for the selected host with the eight-desk Room Full state; retain the live Office across admitted topology updates and boundedly retry exact focus of the new terminal, while cancellation/failure preserves the prior selection, focused Inspector context and every existing selected-host conversation window, and verify no action switches hosts implicitly or invents Office-only runtime state
- [x] 10.7 Restore optional provider configuration/health and bounded authoritative Office boards through the World service, expose one settings dialog above every active view, and verify absent or failed providers leave core topology and terminals intact
- [x] 10.8 Verify Office at desktop and phone widths over unequal selected-host projections, including active/stale states, docked and floating Inspectors, common-header search, native touch panning from non-actionable scene areas, menu-owned Office controls, scene scrolling and preference restoration before checking task 3.3

## 11. Connected Tree migration

- [x] 11.1 Move the checkpoint connected branch diagram from Graph to canonical Tree and replace the list-primary desktop composition, preserving exact host-space-agent/terminal connectors after Office acceptance and consuming one bounded projection in both visual and semantic presentations
- [x] 11.2 Complete Tree search, independent disclosure and dense/unequal branch layout within the one-selected-host, 128-space and 16-leaf-per-space bounds; verify relevance priority, exact global and branch omission counts, clearing search restores prior disclosure, and no dangling connectors or page overflow
- [x] 11.3 Integrate selected-host Inspector conversations with one exact actionable leaf expanded as Tree's inline dock and complete Dock out/Dock in transfer to the shared floating registry, with an equivalent bounded compact/assistive hierarchy; verify stale targets expose no operational action, another host with the same display label never enters the selected-host projection, and admitted task summaries, inline resource sizing, connectors and keyboard/touch operation remain coherent

## 12. Spatial Graph migration

- [x] 12.1 Port the qualified host-first Graph projection and deterministic force layout with explicit host/space/leaf bounds and exact overflow counts, and verify duplicate native identifiers never merge
- [x] 12.2 Restore node dragging/pinning, bounded pan/zoom, Fit, search, disclosure and validated camera/position persistence, and verify status-only updates do not reset settled layout state
- [x] 12.3 Integrate docked and floating selected-host Inspector contexts, semantic hierarchy and qualified conversation connectors, exclude inactive-host identities from the selected-host presentation, and verify canvas and semantic selection/actions stay synchronized
- [x] 12.4 Restore renderer lifecycle and performance bounds for visibility, resize, topology updates and unmount, and verify repeated view changes release work without adding subscriptions or terminal resize traffic

## 13. Complete-view acceptance and delivery

- [x] 13.1 Exercise aggregate selected-host projection, stale/reconnect and colliding-ID cases through the deterministic connection, store and mounted-browser suites; verify Office, Tree, Graph, their search/counts/actions and bounded Inspector registry contain only the selected host and explicit switching retires outgoing contexts before admitting another host. Track the expanded simultaneous live local-plus-SSH matrix in GitHub issue #95
- [x] 13.2 Run focused unit and mounted-browser suites plus synthetic desktop/phone visual comparison for every view, and record the delivered per-view feature inventory with approved breaking retirements identified explicitly
- [x] 13.3 Reconcile README, screenshots, feature/architecture guidance, changelog and current specs with the delivered views, record the deferred workflows in GitHub issue #95, and remove incomplete-checkpoint claims from the delivery narrative
- [x] 13.4 Run the complete repository and distribution checks, resolve current findings and return PR #93 to ready status for independent review without merging it
