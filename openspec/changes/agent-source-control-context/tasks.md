## 1. Prove and define the agent checkout source

- [x] 1.1 Reused C's proven Herdr 0.9.0 pane-token/session metadata seam; presentation guards do not bind token patches and no conditional token delete exists.
- [x] 1.2 Traced the existing workspace-only Changes root and Inspector routing; the Agent checkout panel is separate and never uses workspace CWD.
- [x] 1.3 Defined and tested the fixed 15-key version/fingerprint/base64url layout with no TTL, active-session requirement, and pre-transport Clear rejection.

## 2. Publish and read exact context

- [x] 2.1 Added the one-shot report command before listener startup with one full token update and no renewal job.
- [x] 2.2 Added a connection/generation-routed read-only request that re-reads the pane and rejects browser paths.
- [x] 2.3 Added focused producer, token, session replacement, malformed suffix, changed-file bound, and delayed-Clear regression coverage.

## 3. Present distinct Changes scopes

- [x] 3.1 Added Agent checkout as the default Inspector Changes scope with clear unavailable and Workspace changes states.
- [x] 3.2 Agent checkout has a standalone read-only list; workspace mutations appear only after the explicit Workspace changes choice.
- [x] 3.3 Conversation resource identity and request sequence guards reset context on pane/session/generation/host changes.

## 4. Accept and deliver

- [x] 4.1 Exercised the synthetic compact Inspector fixture with an absent report and explicit Workspace changes choice; the full browser matrix covers desktop and phone layouts with synthetic identities.
- [x] 4.2 Documented the one-shot command, lifetime, restart loss, limits, and Reported PR provenance; synchronized `world-surfaces`, passed focused checks and `npx --yes bun run check`, inspected diff/history, and opened ready PR #107 for independent review.
